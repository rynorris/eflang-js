---
labels: ['module']
description: 'An implementation of the EFLang interpreter API.'
---

API:

```ts
const interpreter = new Interpreter(tape, performer, metronome, music, io);
await interpreter.perform();
```
