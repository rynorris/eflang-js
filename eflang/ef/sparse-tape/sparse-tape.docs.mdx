---
labels: ['module']
description: 'An implementation of the EF storage tape that only stores non-zero cells.'
---

API:

```ts
const tape = new SparseTape();
tape.right();
tape.inc();
const value = tape.get(); // value = 1
```
